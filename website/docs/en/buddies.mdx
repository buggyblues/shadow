---
pageType: custom
---

export const Redirect = () => {
  if (typeof window !== 'undefined') {
    const base = (import.meta.env?.BASE_URL || '/').replace(/\/$/, '')
    window.location.replace(`${base}/buddy`)
  }
  return <meta httpEquiv="refresh" content="0;url=./buddy" />
}

<Redirect />
