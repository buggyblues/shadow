---
title: Play Launch
---

import { Step, Tip, FeatureCard } from '../../../components/ProductComponents'
import { RocketIcon, MessageSquareIcon, CoinIcon, ShieldIcon } from '../../../components/Icons'

# Play Launch

Shadow homepage plays are designed for "click and start" onboarding. A new user can pick a play,
sign in with email code or OAuth, watch a short preparation screen, and land directly in the right
channel or Cloud-backed Buddy workspace.

## What Happens After You Click a Play

<Step num={1} title="Choose a play">Pick a public community play, a private room play, or a Cloud template play from the homepage.</Step>
<Step num={2} title="Sign in quickly">Use email code, Google, or GitHub. Invite codes are not required just to register or explore community features.</Step>
<Step num={3} title="Prepare your space">Shadow starts the configured action: joining an existing server, creating your private channel, or deploying a Cloud template.</Step>
<Step num={4} title="Enter the channel">When the space is ready, Shadow opens the default channel and Buddy greets you there.</Step>

## Play Types

<div className="grid grid-cols-1 sm:grid-cols-2 gap-3 my-4">
  <FeatureCard icon={<MessageSquareIcon className="w-5 h-5" />} title="Public server + private channel" desc="Join an existing public server, then get a private channel named for you. The configured Buddy is already part of that server." />
  <FeatureCard icon={<RocketIcon className="w-5 h-5" />} title="Cloud template deployment" desc="Deploy a git-tracked template with server, Buddy, scripts, and runtime material already attached." />
  <FeatureCard icon={<ShieldIcon className="w-5 h-5" />} title="Member-gated actions" desc="Visitors can explore community flows. Cloud deployment and new server creation require member capabilities." />
  <FeatureCard icon={<CoinIcon className="w-5 h-5" />} title="Shrimp Coins" desc="New users receive starter coins. The first lightweight Cloud experience can be covered by the welcome balance; later use follows wallet or task-center flows." />
</div>

## Cloud Plays

Cloud plays are not fake community rooms. They run real templates from the Shadow Cloud catalog.
The deployment page lets you choose:

- **Official provider** — uses Shadow's OpenAI-compatible model proxy and bills your wallet in Shrimp Coins.
- **My provider** — uses your own encrypted provider profile, so model usage is paid directly to that provider.

The official upstream key stays on Shadow servers and is never injected into your deployed server or
Buddy runtime.

<Tip>When a play finishes, Shadow opens the configured default channel. If a server has no channel yet, the app shows an empty state instead of a broken route.</Tip>

---

- [Quick Start](/product/quick-start) — choose the fastest onboarding path
- [Shrimp Coins](/product/shrimp-coins) — wallet, tasks, and model usage
- [AI Assistants](/product/ai-assistants) — how Buddies work in channels
