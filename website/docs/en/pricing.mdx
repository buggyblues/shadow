---
pageType: custom
---

import { PublicFooter } from '../../components/Layout'
import { CheckIcon } from '../../components/Icons'

export const GITHUB_URL = 'https://github.com/buggyblues/shadow'

export const PricingPage = () => {
  const base = (typeof import.meta !== 'undefined' && import.meta.env?.BASE_URL || '/').replace(/\/$/, '')

  const plans = [
    {
      name: 'Community',
      price: 'Free',
      desc: 'Open-source, free for everyone',
      color: 'border-gray-200',
      badge: null,
      btn: 'bg-gray-800 text-white hover:bg-gray-700',
      btnText: 'Get Started',
      btnHref: GITHUB_URL,
      features: [
        'Full product features',
        'Unlimited servers & channels',
        'AI Buddy integration',
        'Community shop & workspace',
        'Buddy P2P marketplace',
        'Community support',
      ],
    },
    {
      name: 'Home',
      price: 'Free',
      desc: 'Self-hosted, full data autonomy',
      color: 'border-green-400',
      badge: null,
      btn: 'bg-gradient-to-r from-green-400 to-green-500 text-gray-900 hover:scale-105',
      btnText: 'Deploy Now',
      btnHref: GITHUB_URL,
      features: [
        'Everything in Community',
        'Self-hosted deployment',
        'Full data autonomy',
        'Privacy protection',
        'Your hardware, your rules',
        'Community support',
      ],
    },
    {
      name: 'Team',
      price: 'Contact Us',
      desc: 'Team collaboration with custom branding',
      popular: true,
      color: 'border-cyan-400',
      badge: 'For Teams',
      btn: 'bg-gradient-to-r from-cyan-400 to-cyan-500 text-gray-900 hover:scale-105',
      btnText: 'Contact Us',
      btnHref: GITHUB_URL,
      features: [
        'Everything in Home',
        'Priority technical support',
        'Custom logo branding',
        'Advanced permission control',
        'Team Buddy sharing',
        'SLA guarantee',
      ],
    },
  ]

  const comparison = [
    {
      category: 'Deployment',
      rows: [
        { label: 'Cloud-hosted (community)', vals: [true, false, false] },
        { label: 'Self-hosted (single node)', vals: [false, true, true] },
        { label: 'Private deployment', vals: [false, true, true] },
      ],
    },
    {
      category: 'Features',
      rows: [
        { label: 'Servers & channels', vals: ['Unlimited', 'Unlimited', 'Unlimited'] },
        { label: 'AI Buddy integration', vals: [true, true, true] },
        { label: 'Buddy marketplace', vals: [true, true, true] },
        { label: 'Community shop', vals: [true, true, true] },
        { label: 'Shared workspace', vals: [true, true, true] },
        { label: 'Custom branding', vals: [false, false, true] },
        { label: 'Team Buddy sharing', vals: [false, false, true] },
      ],
    },
    {
      category: 'Data & Privacy',
      rows: [
        { label: 'Data autonomy', vals: [false, true, true] },
        { label: 'Privacy protection', vals: ['Standard', 'Full', 'Full'] },
        { label: 'Role-based permissions', vals: ['Basic', 'Basic', 'Advanced'] },
      ],
    },
    {
      category: 'Support',
      rows: [
        { label: 'Community support', vals: [true, true, true] },
        { label: 'Priority support', vals: [false, false, true] },
        { label: 'SLA guarantee', vals: [false, false, true] },
      ],
    },
  ]

  const faqs = [
    { q: 'Is Shadow really free?', a: 'Yes! Shadow is fully open-source. All product features are free. The Home plan lets you self-host for free. The Team plan provides custom branding, priority support, and advanced team features.' },
    { q: 'What is the difference between Home and Team?', a: 'Home is for privacy-conscious individuals who want to self-host and own their data. Team adds custom branding, priority support, advanced permissions, and team-wide Buddy sharing for small teams.' },
    { q: 'How do I self-host Shadow?', a: 'Clone the GitHub repo and follow the deployment guide. Docker Compose setup gets you running in minutes on your own hardware.' },
    { q: 'Can I try Shadow before self-hosting?', a: 'Absolutely! The Community version runs on our cloud with all features. You can sign up and start using it immediately, then migrate to self-hosted whenever you want.' },
    { q: 'What are Shrimp Coins?', a: 'Shrimp Coins are the in-platform currency. Earn them by completing tasks, receive them as gifts, or purchase them. Use them to rent Buddies, buy shop items, and participate in community activities.' },
  ]

  const tierNames = ['Community', 'Home', 'Team']

  return (
    <div className="shadow-page" style={{ fontFamily: "'Nunito', 'ZCOOL KuaiLe', sans-serif" }}>

      {/* Hero */}
      <section className="pt-10 pb-10 px-8 md:px-16 max-w-6xl mx-auto text-center">
        <h1 className="zcool text-4xl md:text-6xl mb-6 leading-tight">Simple, Transparent Pricing</h1>
        <p className="text-lg md:text-xl font-bold max-w-2xl mx-auto" style={{ color: 'var(--shadow-text-muted)' }}>
          Free and open source. Self-host for free or get team features with custom branding.
        </p>
      </section>

      {/* Plan cards */}
      <section className="max-w-6xl mx-auto px-8 md:px-16 pb-20">
        <div className="grid grid-cols-1 md:grid-cols-3 gap-8 items-start">
          {plans.map((p) => (
            <div key={p.name} className={`glass-card border-3 ${p.color} rounded-3xl p-7 relative hover:-translate-y-2 hover:shadow-xl transition-all ${p.popular ? 'md:-mt-4 md:mb-4 shadow-lg' : ''}`}>
              {p.badge && (
                <div className="absolute -top-3.5 left-1/2 -translate-x-1/2 bg-cyan-500 text-white text-xs font-bold px-4 py-1 rounded-full">{p.badge}</div>
              )}
              <div className="text-center mb-6">
                <h3 className="zcool text-2xl font-bold mb-2">{p.name}</h3>
                <div className="flex items-baseline justify-center gap-1 mb-2">
                  <span className="zcool text-4xl font-bold">{p.price}</span>
                </div>
                <p className="text-sm font-medium" style={{ color: 'var(--shadow-text-dim)' }}>{p.desc}</p>
              </div>
              <ul className="space-y-3 mb-8">
                {p.features.map((f) => (
                  <li key={f} className="flex items-start gap-2">
                    <CheckIcon className="w-5 h-5 text-cyan-500 shrink-0 mt-0.5" />
                    <span className="font-medium text-sm" style={{ color: 'var(--shadow-text-muted)' }}>{f}</span>
                  </li>
                ))}
              </ul>
              <a
                href={p.btnHref}
                target="_blank"
                rel="noreferrer"
                className={`block text-center font-bold py-3 rounded-full border-2 border-gray-800 transition-all zcool ${p.btn}`}
                style={{ textDecoration: 'none' }}
              >
                {p.btnText}
              </a>
            </div>
          ))}
        </div>
      </section>

      {/* Feature comparison table */}
      <section className="max-w-6xl mx-auto px-8 md:px-16 pb-20">
        <h2 className="zcool text-3xl text-center mb-10">Feature Comparison</h2>
        <div className="glass-card rounded-3xl overflow-hidden border-2" style={{ borderColor: 'var(--shadow-card-border)' }}>
          <div className="overflow-x-auto">
            <table className="w-full text-sm" style={{ borderCollapse: 'collapse' }}>
              <thead>
                <tr style={{ borderBottom: '2px solid var(--shadow-card-border)' }}>
                  <th className="text-left p-4 font-bold" style={{ color: 'var(--shadow-text)', minWidth: '200px' }}>Feature</th>
                  {tierNames.map((t) => (
                    <th key={t} className="p-4 text-center font-bold zcool text-lg" style={{ color: 'var(--shadow-text)', minWidth: '120px' }}>{t}</th>
                  ))}
                </tr>
              </thead>
              <tbody>
                {comparison.map((group) => (
                  <>
                    <tr key={group.category}>
                      <td colSpan={4} className="px-4 pt-5 pb-2 font-black text-xs uppercase tracking-wider" style={{ color: 'var(--shadow-text-dim)' }}>
                        {group.category}
                      </td>
                    </tr>
                    {group.rows.map((row) => (
                      <tr key={row.label} style={{ borderBottom: '1px solid var(--shadow-card-border)' }}>
                        <td className="p-4 font-medium" style={{ color: 'var(--shadow-text-muted)' }}>{row.label}</td>
                        {row.vals.map((v, i) => (
                          <td key={i} className="p-4 text-center">
                            {v === true ? (
                              <CheckIcon className="w-5 h-5 text-cyan-500 mx-auto" />
                            ) : v === false ? (
                              <span style={{ color: 'var(--shadow-text-dim)' }}>—</span>
                            ) : (
                              <span className="font-bold text-sm" style={{ color: 'var(--shadow-text-muted)' }}>{v}</span>
                            )}
                          </td>
                        ))}
                      </tr>
                    ))}
                  </>
                ))}
              </tbody>
            </table>
          </div>
        </div>
      </section>

      {/* FAQ */}
      <section className="max-w-4xl mx-auto px-8 md:px-16 pb-20">
        <h2 className="zcool text-3xl text-center mb-10">FAQ</h2>
        <div className="space-y-4">
          {faqs.map((item) => (
            <div key={item.q} className="glass-card border-2 rounded-2xl p-6" style={{ borderColor: 'var(--shadow-card-border)' }}>
              <h4 className="font-bold mb-2" style={{ color: 'var(--shadow-text)' }}>{item.q}</h4>
              <p className="font-medium text-sm" style={{ color: 'var(--shadow-text-muted)' }}>{item.a}</p>
            </div>
          ))}
        </div>
      </section>

      <PublicFooter lang="en" />
    </div>
  )
}

<PricingPage />
