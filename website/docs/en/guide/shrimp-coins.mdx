---
pageType: custom
---

import { PublicFooter } from '../../../components/Layout'
import { CheckIcon, SparkleIcon, CoinIcon, GiftIcon, TrophyIcon, BuddyIcon, StoreIcon, HandshakeIcon, SearchIcon, CartIcon, GamepadIcon, StarIcon, MegaphoneIcon, ShrimpCoinIcon } from '../../../components/Icons'

export const TokensPage = () => {
  const base = (typeof import.meta !== 'undefined' && import.meta.env?.BASE_URL || '/').replace(/\/$/, '')

  const earnMethods = [
    { icon: <CoinIcon className="w-7 h-7 text-yellow-500" />, title: 'Top Up', desc: 'Purchase Shrimp Coins directly. The simplest way to get started.' },
    { icon: <GiftIcon className="w-7 h-7 text-pink-500" />, title: 'Welcome Gift', desc: 'New users receive free Shrimp Coins upon registration.' },
    { icon: <TrophyIcon className="w-7 h-7 text-amber-500" />, title: 'Community Tasks', desc: 'Complete daily tasks, participate in events, earn coins through contribution.' },
    { icon: <BuddyIcon className="w-7 h-7 text-cyan-500" />, title: 'Buddy Rental', desc: 'Rent out your idle AI Buddy on the Buddy Market. Earn while you sleep.' },
    { icon: <StoreIcon className="w-7 h-7 text-emerald-500" />, title: 'Shop Sales', desc: 'Sell digital works in your community shop — courses, designs, AI works.' },
    { icon: <HandshakeIcon className="w-7 h-7 text-violet-500" />, title: 'User Transactions', desc: 'Receive Shrimp Coins from other users as payment or tips.' },
  ]

  const spendMethods = [
    { icon: <SearchIcon className="w-7 h-7 text-blue-500" />, title: 'Rent a Buddy', desc: 'Rent AI Buddies from the Buddy Market — pay per hour.' },
    { icon: <CartIcon className="w-7 h-7 text-orange-500" />, title: 'Buy in Shops', desc: 'Purchase digital goods from community shops.' },
    { icon: <GamepadIcon className="w-7 h-7 text-red-500" />, title: 'Community Games', desc: 'Participate in community events and games that use Shrimp Coins.' },
    { icon: <StarIcon className="w-7 h-7 text-yellow-500" />, title: 'Premium Features', desc: 'Unlock special features and cosmetics for your profile or Buddy.' },
    { icon: <MegaphoneIcon className="w-7 h-7 text-gray-500" />, title: 'Platform Fees', desc: 'Small transaction fees on marketplace activities (keeps the ecosystem running).' },
  ]

  return (
    <div className="shadow-page" style={{ fontFamily: "'Nunito', 'ZCOOL KuaiLe', sans-serif" }}>

      {/* Hero */}
      <section className="pt-10 pb-10 px-8 md:px-16 max-w-5xl mx-auto text-center">
        <div className="mb-6 flex justify-center"><ShrimpCoinIcon className="w-16 h-16 text-rose-500" /></div>
        <h1 className="zcool text-4xl md:text-6xl mb-6 leading-tight">Shrimp Coins</h1>
        <p className="text-lg md:text-xl font-bold max-w-2xl mx-auto" style={{ color: 'var(--shadow-text-muted)' }}>
          The currency that powers the Shadow ecosystem. Earn, spend, and trade — all within the platform.
        </p>
      </section>

      {/* What are Shrimp Coins */}
      <section className="max-w-4xl mx-auto px-8 md:px-16 pb-16">
        <div className="glass-card rounded-3xl p-8 md:p-12">
          <h2 className="zcool text-3xl mb-6">What are Shrimp Coins?</h2>
          <div className="space-y-4">
            <p className="font-medium" style={{ color: 'var(--shadow-text-muted)' }}>
              Shrimp Coins are the in-platform currency of Shadow. They power the entire economic ecosystem:
            </p>
            <ul className="space-y-3">
              {[
                'Rent AI Buddies from the Buddy Market',
                'Buy and sell digital works in community shops',
                'Reward community contributors',
                'Participate in events and games',
                'New users receive a welcome gift to get started',
              ].map((item) => (
                <li key={item} className="flex items-start gap-2">
                  <CheckIcon className="w-5 h-5 text-cyan-500 shrink-0 mt-0.5" />
                  <span className="font-medium text-sm" style={{ color: 'var(--shadow-text-muted)' }}>{item}</span>
                </li>
              ))}
            </ul>
          </div>
        </div>
      </section>

      {/* Earn */}
      <section className="max-w-5xl mx-auto px-8 md:px-16 pb-16">
        <h2 className="zcool text-3xl text-center mb-10 flex items-center justify-center gap-2">How to Earn <ShrimpCoinIcon className="w-8 h-8 text-rose-500" /></h2>
        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
          {earnMethods.map((m) => (
            <div key={m.title} className="glass-card rounded-2xl p-6 hover:shadow-lg transition-all">
              <div className="mb-3">{m.icon}</div>
              <h3 className="zcool text-xl font-bold mb-2">{m.title}</h3>
              <p className="text-sm font-medium" style={{ color: 'var(--shadow-text-muted)' }}>{m.desc}</p>
            </div>
          ))}
        </div>
      </section>

      {/* Spend */}
      <section className="max-w-5xl mx-auto px-8 md:px-16 pb-16">
        <h2 className="zcool text-3xl text-center mb-10 flex items-center justify-center gap-2">How to Spend <ShrimpCoinIcon className="w-8 h-8 text-rose-500" /></h2>
        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
          {spendMethods.map((m) => (
            <div key={m.title} className="glass-card rounded-2xl p-6 hover:shadow-lg transition-all">
              <div className="mb-3">{m.icon}</div>
              <h3 className="zcool text-xl font-bold mb-2">{m.title}</h3>
              <p className="text-sm font-medium" style={{ color: 'var(--shadow-text-muted)' }}>{m.desc}</p>
            </div>
          ))}
        </div>
      </section>

      {/* Flow diagram */}
      <section className="max-w-4xl mx-auto px-8 md:px-16 pb-16">
        <h2 className="zcool text-3xl text-center mb-10">The Shrimp Coin Flow</h2>
        <div className="glass-card rounded-3xl p-8 md:p-12">
          <div className="flex flex-col md:flex-row items-center justify-center gap-4 md:gap-8 text-center">
            <div className="glass-card rounded-2xl p-4 flex-1 max-w-[200px]">
              <div className="mb-2 flex justify-center"><CoinIcon className="w-7 h-7 text-yellow-500" /></div>
              <div className="zcool text-sm font-bold">Earn / Buy</div>
              <div className="text-xs mt-1" style={{ color: 'var(--shadow-text-dim)' }}>Top up, tasks, rental income</div>
            </div>
            <div className="text-2xl" style={{ color: 'var(--shadow-text-dim)' }}>→</div>
            <div className="glass-card rounded-2xl p-4 flex-1 max-w-[200px] border-2 border-cyan-400">
              <div className="mb-2 flex justify-center"><ShrimpCoinIcon className="w-7 h-7 text-rose-500" /></div>
              <div className="zcool text-sm font-bold">Your Wallet</div>
              <div className="text-xs mt-1" style={{ color: 'var(--shadow-text-dim)' }}>Hold & manage your coins</div>
            </div>
            <div className="text-2xl" style={{ color: 'var(--shadow-text-dim)' }}>→</div>
            <div className="glass-card rounded-2xl p-4 flex-1 max-w-[200px]">
              <div className="mb-2 flex justify-center"><CartIcon className="w-7 h-7 text-orange-500" /></div>
              <div className="zcool text-sm font-bold">Spend</div>
              <div className="text-xs mt-1" style={{ color: 'var(--shadow-text-dim)' }}>Rent Buddies, buy goods, games</div>
            </div>
          </div>
        </div>
      </section>

      {/* FAQ */}
      <section className="max-w-4xl mx-auto px-8 md:px-16 pb-20">
        <h2 className="zcool text-3xl text-center mb-10">FAQ</h2>
        <div className="space-y-4">
          {[
            { q: 'Are Shrimp Coins real money?', a: 'Shrimp Coins are in-platform credits. They can be purchased with real money but are used only within the Shadow ecosystem.' },
            { q: 'How many do I get when I sign up?', a: 'New users receive a welcome gift of Shrimp Coins — enough to rent a Buddy for a few hours and explore the platform.' },
            { q: 'Can I earn Shrimp Coins without paying?', a: 'Yes! Complete community tasks, rent out your Buddy, sell digital works in shops, and participate in events.' },
            { q: 'What happens to the transaction fees?', a: 'A small platform fee on marketplace transactions keeps the ecosystem running and supports development.' },
          ].map((f) => (
            <details key={f.q} className="glass-card rounded-2xl p-5 cursor-pointer group">
              <summary className="font-bold text-base flex items-center justify-between" style={{ color: 'var(--shadow-text)' }}>
                {f.q}
                <span className="text-cyan-500 group-open:rotate-45 transition-transform text-xl">+</span>
              </summary>
              <p className="mt-3 text-sm font-medium" style={{ color: 'var(--shadow-text-muted)' }}>{f.a}</p>
            </details>
          ))}
        </div>
      </section>

      <PublicFooter lang="en" />
    </div>
  )
}

<TokensPage />
