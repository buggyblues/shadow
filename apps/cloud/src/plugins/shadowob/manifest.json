{
  "id": "shadowob",
  "name": "Shadow",
  "description": "Connect agents to Shadow chat platform with servers, channels, and buddy avatars",
  "version": "1.0.0",
  "category": "communication",
  "icon": "message-circle",
  "website": "https://shadow.ob",
  "auth": {
    "type": "token",
    "fields": [
      {
        "key": "SHADOW_SERVER_URL",
        "label": "Shadow Server URL",
        "description": "Shadow platform server base URL",
        "required": true,
        "sensitive": false,
        "placeholder": "https://shadow.example.com"
      },
      {
        "key": "SHADOWOB_API_TOKEN",
        "label": "Shadow API Token",
        "description": "Your Shadow platform API token for provisioning",
        "required": false,
        "sensitive": true
      }
    ]
  },
  "config": {
    "type": "object",
    "properties": {
      "servers": {
        "type": "array",
        "description": "Shadow servers to provision"
      },
      "buddies": {
        "type": "array",
        "description": "Buddy agents to create"
      },
      "bindings": {
        "type": "array",
        "description": "Binding rules connecting buddies to agents and channels"
      },
      "greeting": {
        "type": "object",
        "description": "Greeting and entry-channel behavior for provisioned Shadow spaces"
      },
      "serverApps": {
        "type": "array",
        "description": "Server Apps to install from the catalog or manifest and grant to provisioned buddies",
        "items": {
          "type": "object",
          "additionalProperties": true,
          "properties": {
            "id": {
              "type": "string",
              "description": "Stable template id for this Server App installation"
            },
            "serverId": {
              "type": "string",
              "description": "Server config id that owns this app"
            },
            "catalogEntryId": {
              "type": "string",
              "description": "Shadow Server App catalog entry id"
            },
            "catalogAppKey": {
              "type": "string",
              "description": "Shadow Server App catalog app key to resolve and install"
            },
            "manifestUrl": {
              "type": "string",
              "description": "Manifest URL fallback when not installing from catalog"
            },
            "manifest": {
              "type": "object",
              "description": "Inline manifest fallback when not installing from catalog"
            },
            "grants": {
              "type": "array",
              "description": "Buddy grants applied after app installation"
            }
          }
        }
      }
    }
  },
  "capabilities": ["channel"],
  "tags": ["communication", "chat", "shadow", "built-in"],
  "popularity": 100
}
